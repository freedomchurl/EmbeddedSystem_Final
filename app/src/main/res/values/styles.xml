<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#0B3B39</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Floating">
        <item name="colorPrimary">@color/normalMainColor</item>
        <item name="colorPrimaryDark">@color/darkMainColor</item>
        <item name="colorAccent">@color/normalMainColor</item>
        <item name="android:colorActivatedHighlight">@color/darkMainColor</item>
    </style>

</resources>
